const ApiVersion = "/bot/evaluation";
const ApiRoutes = {
    // Risk
    RiskSubmit: ApiVersion + "/submit",
    EvaluationNonTechnicalQuestions: ApiVersion + "/non-tech/questions",
};

export default ApiRoutes;
